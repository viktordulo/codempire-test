import { ActionInterface } from './../interfaces';
import { Store } from "redux";

const initialState = {}

const buttonsReducer = (state: any = initialState, action: ActionInterface) => {

    return state;
}

export default buttonsReducer;